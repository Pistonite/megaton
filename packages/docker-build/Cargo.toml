[package]
name = "megaton-docker-build"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
cu = { workspace = true, features = ["fs"] }

megaton-toolchain-build.path = "../toolchain-build"
megaton-cli-build.path = "../cli-build"

[[example]]
name = "prep_toolchain_build"
[[example]]
name = "prep_megaton_cmd"

