version: '3'

includes:
  cargo: 
    taskfile: ../../mono-dev/task/cargo.yaml
    internal: true
    optional: true

tasks:
  check:
    - layered-crate
    - task: cargo:clippy-all
    - task: cargo:fmt-check
  fix:
    - task: cargo:fmt-fix
  build:
    - cargo build
  test:
    - cargo test
